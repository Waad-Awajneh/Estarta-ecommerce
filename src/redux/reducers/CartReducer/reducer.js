import * as CONSTANTS from "./constant";

const initialState = {};

export default function CartReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
